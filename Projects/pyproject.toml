[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "projects"
version = "0.1.0"
dependencies = [
    "pandas",
    "sqlalchemy",
    "python-dotenv"
]

[tool.setuptools.packages.find]
where = ["."]
